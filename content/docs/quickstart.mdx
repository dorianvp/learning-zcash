---
title: Quickstart
icon: HomeIcon
---

This guide will use [the `zcash-lib` crate](https://github.com/dorianvp/zcash-lib).

Clone the repository:

```bash
git clone git@github.com:dorianvp/zcash-lib.git
```

Install dependencies:

```bash
cd zcash-lib
cargo build
```

And start hacking!
